"""Pydantic validation schemas and SQLAlchemy models for lead data.

This package defines the structure and validation logic for leads as they 
pass through the system. It handles the mapping between raw webhook JSON, 
enriched firmographic data, and the persistent database state.
"""
